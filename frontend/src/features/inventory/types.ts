export type MovementType = 'ENTRY' | 'CONSUMPTION' | 'WASTE' | 'ADJUSTMENT_IN' | 'ADJUSTMENT_OUT';
export interface Supplier { id: string; name: string; contact: string | null; phone: string | null; email: string | null; notes: string | null; active: boolean; version: number }
export interface Lot { id: string; number: string | null; expiryDate: string | null; receivedAt: string; unitCost: string | null; initialQuantity: string; supplier: Supplier | null; available: string; expiryStatus: string; expiryLabel: string | null }
export interface Product { id: string; name: string; description: string | null; category: string; unit: string; minimumStock: string; supplierId: string | null; supplier: Supplier | null; referenceCost: string | null; active: boolean; version: number; lots: Lot[]; stock: string; usableStock: string; expiredStock: string; stockStatus: string; nextExpiry: string | null; expiring: boolean; expired: boolean }
export interface Movement { id: string; productId: string; lotId: string; type: MovementType; quantity: string; effectiveDate: string; reason: string; note: string | null; createdAt: string; createdBy: { user: { firstName: string; lastName: string } }; lot: { number: string | null; product: { name: string; unit: string } } }
export interface InventoryData { today: string; timeZone: string; canManage: boolean; expiryWarningDays: number; products: Product[]; suppliers: Supplier[]; movements: Movement[]; summary: { active: number; low: number; empty: number; expiring: number; expired: number } }
export interface Detail { today: string; timeZone: string; canManage: boolean; product: Product; movements: Movement[] }
